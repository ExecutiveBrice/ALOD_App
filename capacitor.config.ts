import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.alod.presences',
  appName: 'ALOD Présences',
  webDir: 'www',
  server: { androidScheme: 'https' },
};

export default config;
